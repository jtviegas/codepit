drop table "COMMON"."ADJECTIVE";
CREATE TABLE "COMMON"."ADJECTIVE" 
   ("ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY NOT NULL, 
	"NAME" varchar(100 BYTE) 
	, primary key("ID")
);
GRANT SELECT,UPDATE,INSERT,DELETE,REFERENCES ON "COMMON"."ADJECTIVE" TO "SPECIAL"; 

drop table "SPECIAL"."REMARK" ;
CREATE TABLE "SPECIAL"."REMARK" 
   ("ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY NOT NULL, 
	"TEXT" varchar(1000 BYTE)
	,  "ADJECTIVE_ID" NUMBER
	, primary key("ID")
	, foreign key("ADJECTIVE_ID") references COMMON.ADJECTIVE("ID") on delete cascade 
);

GRANT SELECT,UPDATE,INSERT,DELETE,REFERENCES ON "SPECIAL"."REMARK" TO "COMMON"; 
	